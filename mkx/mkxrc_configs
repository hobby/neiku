# vim:ts=4:sw=4:ft=conf:expandtab

# pre-make: execute before make (for mkd)
pre-make = echo this is pre-make code

# post-make: execute after make (for mkd)
post-make = echo this is post-make code

# make-flags: make flags (for mk)
make-flags = "-j`cat /proc/cpuinfo | grep processor | wc -l`"

# custom-deployer: custom module's deployer (for mkd)
custom-deployer:

# custom-runner: custom module's runner (for mkrun)
custom-runner:
